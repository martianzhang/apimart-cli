    Refactor CLI to support generic OpenAI-compatible APIs

    - Add dual env var support: OPENAI_* alongside APIMART_*
    - Add sync image generation mode for OpenAI/OpenRouter
    - Add auto-detection of async vs sync mode by API domain
    - Support OpenAI /v1/models as an alternative model source
    - Rename --provider flag to --mode with sync/async/auto values
    - Update file naming from apimart_* to image_*/video_*
    - Add OpenRouter-specific header support
    - Update .gitignore and config files accordingly
